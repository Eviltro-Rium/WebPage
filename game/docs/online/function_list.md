|功能清单|实现状态|
|-------|--------|
|在游戏主界面增加在线对决选项|已实现（MVP）|
|点击进入在线对决子页面后，用户需要输入昵称|已实现（MVP）|
|玩家可以在子页面选择创建房间或加入房间（基于 WebRTC P2P）|已实现（MVP）|
|房间号为4位字符（数字+字母），加入房间时需输入房间号，未找到房间号时显示未找到对应房间，并让玩家重试或返回|已实现（MVP）|
|进入房间显示两位玩家信息（昵称），用随机emoji作为头像，在房间等待时，玩家需要点击准备按钮，可取消准备，双方可见彼此的准备状态，双方准备好后进入游戏|已实现（MVP）|
|进入游戏后，双方先选择使用角色，只能为主角，对手不可见，但可以看见对手是否选择完毕，玩家选中角色点击确定后标识确认|已实现（MVP）|
|双方都确定后，进入对战，经典1v1对战（用p2p实现联机，游戏模板参照单机的1v1），对手卡牌不可见，开局之前用12面骰决定谁先手|部分实现：复用单机 Engine；先手在 MVP 中由主机随机决定，下一步可接入 CombatRuntime 12 面骰事件|
|游戏结束后返回准备阶段的房间，等待双方准备好后进行下一局|已实现（MVP）：结算后可由任一方返回准备大厅|
|联机游戏过程显示玩家在线状态，可随时退出返回游戏主界面，当双方都离线时销毁空房间|部分实现：显示连接状态并支持退出；Durable Object 空闲清理与重连待补充|

## 前端与环境

|功能清单|实现状态|
|-------|--------|
|在线对决页面仅允许在 HTTPS 环境运行，检测不支持 WebRTC 的浏览器并给出提示|已实现（MVP）|
|静态网页通过独立的信令服务地址连接房间，不把信令逻辑混入单机战斗引擎|已实现（MVP）|
|配置生产环境与本地开发环境的信令地址、协议版本和 ICE 服务器|部分实现：信令地址由管理员配置，当前固定公开 STUN，尚未引入版本协商|
|页面离开、刷新和浏览器关闭时主动发送离开消息并清理本地连接状态|已实现（MVP）：关闭 DataChannel/WebSocket 并由 Worker 广播离开|

## 房间与大厅

|功能清单|实现状态|
|-------|--------|
|创建房间时生成不可预测的房间号和房主身份令牌|部分实现：4 位随机房间码，身份由 WebSocket peerId 标识，令牌机制待补充|
|房间号冲突时自动重新生成，房间人数达到 2 人后禁止第三人加入|已实现（MVP）：Worker 限制每房间两条连接|
|加入房间时校验房间号格式、房间是否存在、房间是否已满以及协议版本是否兼容|部分实现：格式/不存在/满员已校验，协议版本协商待补充|
|昵称长度、空昵称、重复昵称和敏感字符校验|部分实现：空值与长度已校验，重复/敏感词待接入|
|房间状态明确区分：等待加入、准备、角色选择、战斗中、结算、再战或关闭|部分实现：等待/准备/战斗/结束/返回大厅已覆盖|
|房主断线后通知另一位玩家，并提供重新创建房间或退出选项|已实现（MVP）：peerLeft 状态提示，可退出或重新创建|
|双方都离线后销毁房间；无人加入的房间超时自动清理|部分实现：连接断开后 DO 不再保留客户端，超时清理待补充|
|房间操作按钮防重复点击，所有操作显示处理中、成功和失败状态|部分实现：非法指令会被主机拒绝，细粒度处理中状态待补充|

## 信令与 WebRTC

|功能清单|实现状态|
|-------|--------|
|使用 WebSocket/WSS 信令交换 offer、answer 和 ICE candidate|已实现（MVP）|
|信令服务使用 Durable Object 或等价的房间隔离机制保存临时连接状态|已实现（MVP）|
|建立连接后切换到 WebRTC DataChannel，游戏事件不经过信令服务转发|已实现（MVP）：优先 DataChannel；直连未建立时自动锁定为信令 WebSocket 中继|
|DataChannel 建立失败、超时或关闭时显示连接失败原因并允许重试|已实现（MVP）：显示当前传输模式，直连未建立时自动使用信令中继|
|配置 STUN 服务器，并支持 TURN 中继作为无法直连时的回退方案|部分实现：配置公开 STUN；按小规模上线要求不使用 TURN，暂以信令 WebSocket 兜底|
|为每位玩家分配稳定的 peerId，禁止客户端伪造另一位玩家身份|部分实现：Worker 分配/校验 peerId，签名令牌待补充|
|心跳、连接质量和最后活动时间监测，显示在线、连接中和已断线状态|部分实现：信令 ping 与 WebRTC connectionState 已显示|
|信令服务校验 Origin、房间令牌和请求频率，避免任意客户端占用房间|部分实现：可用 ALLOWED_ORIGINS 限制来源并限制消息大小，令牌/频率限制待补充|

## 联机协议与状态同步

|功能清单|实现状态|
|-------|--------|
|P2P 适配器实现与本地 1v1 相同的 start、dispatch、snapshot、subscribe 和 close 接口|已实现（MVP）：在线 CombatSession 复用单机 GameUI，底层可在 P2P/信令中继间选择|
|统一定义 hello、ready、selectCharacter、command、event、snapshot、ack、ping 和 leave 消息||
|每条战斗消息包含协议版本、房间号、peerId、递增序号和幂等请求 ID||
|主机运行权威战斗引擎，客户端只能提交出牌、使用道具、选色和结束回合等指令||
|主机验证指令是否合法后再广播 CombatEvent，拒绝非法或过期指令||
|事件按序处理，重复消息不重复执行，丢失消息可通过 ack 或快照恢复||
|定期同步 CombatState 快照，检测双方牌堆、回合和生命值是否出现偏差||
|随机数统一使用带种子的 CombatRuntime，12 面骰和所有随机效果在双方结果一致||
|公共状态与私有状态分离，只向玩家发送自己的手牌、牌库信息和允许公开的对手信息||
|同步黑牌选色、战利白卡、Buff、不可防御伤害、抽牌和弃牌等全部现有卡牌效果||
|网络延迟期间锁定当前操作，避免同一张牌或同一回合被重复提交||
|断线重连后通过最近快照和事件序号恢复战斗，不允许回退已经结算的动作||

## 角色与战斗流程

|功能清单|实现状态|
|-------|--------|
|角色选择结果只对本人公开，对手仅看到“已选择”状态||
|双方角色确定后由主机统一投掷 12 面骰决定先手并广播结果|已实现（MVP）|
|联机战斗复用经典 1v1 的卡牌、状态注册表、目标选择和事件动画|已实现（MVP）：复用 Engine 与 CombatEvent；在线页面日志显示事件|
|对手手牌、牌库顶和隐藏判定牌不出现在本地 UI 或调试日志中||
|目标选择、颜色选择和多选弹窗由当前玩家操作，结果通过指令提交||
|所有出牌动画和飘字以已确认的 CombatEvent 播放，不能由本地点击直接伪造||
|回合超时、玩家主动跳过、非法操作和对手退出都有明确的结束路径||

## 结算与再战

|功能清单|实现状态|
|-------|--------|
|双方收到同一份胜负、伤害、剩余生命和回合统计结果||
|结算界面等待双方确认后返回准备阶段，不因单方刷新而重复结算||
|再战时重新初始化本局牌堆和状态，不污染上一局的临时战斗数据||
|一方选择退出、再战或返回主页时同步通知另一方||
|网络中断后恢复时校验房间仍处于同一局，已结束的房间不可继续出牌||

## 安全、测试与运维

|功能清单|实现状态|
|-------|--------|
|房间令牌和重连令牌使用短期有效期，不能仅凭房间号控制房间||
|服务端限制房间创建、加入、消息大小和发送频率||
|客户端不信任对手发送的生命值、牌堆和伤害结果||
|建立本地双浏览器、跨设备、跨 Wi-Fi、手机流量和 TURN 回退测试||
|覆盖建房、加入、准备、选角、所有卡牌事件、断线、刷新、重连、结算和再战的自动化测试||
|记录连接失败率、平均延迟、房间存活时间和异常断线原因，但不记录玩家手牌等隐私数据||
|提供信令服务健康检查、错误日志和房间清理监控||
